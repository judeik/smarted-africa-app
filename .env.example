# Environment variables for SmartEd Africa (Vite)
# Rename to .env.local for local development and don't commit secrets to git
VITE_API_BASE_URL=https://api.smarted.africa
VITE_AUTH_PROVIDER=basic
VITE_FEATURE_OFFLINE=true
VITE_DEFAULT_LOCALE=en
